# Default values for zcm-base.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  repository: europe-west2-docker.pkg.dev/wct-oss-dev
  namespace: zcm9
  nodeSelector:
    cloud.google.com/gke-nodepool: zcm-pool
  architecture: ha
