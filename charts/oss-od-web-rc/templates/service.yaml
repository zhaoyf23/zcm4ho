apiVersion: v1
kind: Service
metadata:
  name: {{ include "oss-od-web-rc.fullname" . }}
  labels:
    {{- include "oss-od-web-rc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oss-od-web-rc.selectorLabels" . | nindent 4 }}
