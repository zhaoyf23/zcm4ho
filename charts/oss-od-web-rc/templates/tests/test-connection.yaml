apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oss-od-web-rc.fullname" . }}-test-connection"
  labels:
    {{- include "oss-od-web-rc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oss-od-web-rc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
